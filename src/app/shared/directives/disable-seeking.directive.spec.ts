import { DisableSeekingDirective } from './disable-seeking.directive';

describe('DisableSeekingDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableSeekingDirective();
    expect(directive).toBeTruthy();
  });
});
